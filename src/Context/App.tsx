import * as React from 'react'
import { Element as ElementModel, Account as AccountModel } from '../api/models'

// @TODO change interfaces only to the ones that are generated by API client generator
export interface AppState {
  api?: Api
  user?: UserState
  accounts?: AccountModel[]
  messages?: Message[]
  data?: ElementModel[]
  setState?: (state: Partial<AppState>) => void
}

export interface UserState {
  jwt?: any
  isAuthenticated?: boolean
  data?: any
}

export interface Api {
  host?: string
  headers?: {
    [key: string]: string
  }
}

export interface Message {
  id: number
  date: string
  message: string
}

export interface MoneyElement {
  amount?: number
  currency?: string
}

export interface BudgetElement extends ElementModel {}

export let AppContext = React.createContext({} as AppState)
